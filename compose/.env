#
# RDSS-Archivematica docker-compose environment variables
#

# Override the docker-compose project name
COMPOSE_PROJECT_NAME=rdss

## Archivematica ##############################################################

# No variables required currently.


## Shibboleth #################################################################

# The domain name to use in certificates and hostnames etc.
# Note you cannot just change this here; you will need to reconfigure IdP, SP,
# CA, LDAP, etc, in many other configuration files too.
DOMAIN_NAME=example.ac.uk

# The organisation that owns the domain. This is used in LDAP and CA config.
DOMAIN_ORGANISATION="Example University"

# The external IP address for the example-idp service
IDP_EXTERNAL_IP=127.0.2.1

# The external IP address for the example-sp ('sp1') service
SP1_EXTERNAL_IP=127.0.3.1

# The external IP address for the nginx service
NGINX_EXTERNAL_IP=127.0.4.1

